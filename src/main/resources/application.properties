spring.application.name=InvestiProjet
spring.datasource.url=jdbc:mysql://localhost:3306/inesvtibd?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
server.port=8088
debug=true

# Configuration de l'email
spring.mail.host=smtp.gmail.com  # Hôte du serveur SMTP de Gmail
# Port pour utiliser SMTP (587 est pour TLS)
spring.mail.port=587  
spring.mail.username=investi.tn@gmail.com  # Votre adresse email
spring.mail.password=dyzzsliblpuhwfsp  # Votre mot de passe d'application Gmail
spring.mail.properties.mail.smtp.auth=true  # Authentification nécessaire
spring.mail.properties.mail.smtp.starttls.enable=true  # Utiliser TLS
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com  # SSL (optionnel)